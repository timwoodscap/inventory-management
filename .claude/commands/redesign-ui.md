---
description: Redesign the app UI into a modern SaaS sidebar layout
---

Invoke the `redesign-ui` skill and follow its instructions to redesign the app's UI into a
modern SaaS-style interface with a left vertical navigation sidebar, CSS design tokens, and
consistent spacing.

Use the Skill tool with skill name `redesign-ui`.
